services:
  heypizza:
    image: caddy:2-alpine
    container_name: heypizza-madrid
    restart: unless-stopped
    ports:
      - "${HEYPIZZA_PORT:-8088}:80"
    volumes:
      - ./index.html:/srv/index.html:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
